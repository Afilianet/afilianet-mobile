name: CI
on:
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: npm
      - run: npm ci
      - run: npm run typecheck
      - run: npm run lint
      - run: npm test -- --runInBand --watch=false --forceExit --runTestsByPath src/tests/app/compliance-document-capture.test.tsx src/tests/app/compliance-document-confirmation.test.tsx src/tests/app/document-pass-details.test.tsx src/tests/app/document-review-status.test.tsx
