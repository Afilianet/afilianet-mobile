Cambio de vista sobre los mismos datos: red por niveles / red completa; pagos del mes / históricos.

```jsx
<Tabs opciones={["Niveles", "Todos"]} valor={vista} onChange={setVista} ancho />
```

Máximo 4 opciones. Con más, usa `Select`.
