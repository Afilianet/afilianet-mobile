Icono tocable sin texto: cerrar, compartir, filtrar, volver.

```jsx
<IconButton etiqueta="Compartir enlace"><img src="/assets/iconos/share-2.svg" width={20} /></IconButton>
```

Mínimo 44 px en móvil. `etiqueta` alimenta `aria-label` y es obligatoria.
