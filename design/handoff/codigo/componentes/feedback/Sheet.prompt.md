Hoja inferior para compartir enlace, confirmar retiro o filtrar la red.

```jsx
<Sheet abierta={abierta} titulo="Compartir tu enlace" onCerrar={cerrar} pie={<Button ancho>Copiar enlace</Button>}>…</Sheet>
```

Requiere un ancestro con `position: relative`. Entra en 240ms desde abajo.
